<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="color-scheme" content="only light" />
  <title>Register</title>

  <!-- Bootstrap & Style -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous" />
  <link rel="stylesheet" href="./style/global.css" />
  <link rel="stylesheet" href="style/main.css" />

  <style>
.btn-primary,
.btn-outline-secondary {
  background-color: #0089ff !important;
  color: white !important;
  border: none !important;
}

body {
    overflow-x: hidden;
}

.btn-primary:hover,
.btn-outline-secondary:hover {
  background-color: #0d6efd !important;
  color: white !important;
}

.nav-link,
.nav-link.active {
  background-color: #0089ff !important;
  color: white !important;
}
  </style>
</head>

<body class="vh-100 d-flex justify-content-center align-items-center bg-body-tertiary">
  <div class="container-fluid">
    <div class="row justify-content-center">
      <div class="col-12 col-sm-10 col-md-8 col-lg-6">
        <!-- Formm Register -->
        <form action="/registered" method="post" class="w-100">
          <img src="/imglogo/iconlogo.png" class="mb-4 mx-auto d-block" width="150" alt="logo" />
          <h1 class="h3 mb-3 text-center">Register Account</h1>

          <div class="form-floating mb-3">
            <input type="text" class="form-control form-control-lg" id="floatingName" name="name" placeholder="Name" />
            <label for="floatingName">Name</label>
          </div>

          <div class="form-floating mb-3">
            <input type="text" class="form-control form-control-lg" id="floatingUsername" name="username" placeholder="Username" />
            <label for="floatingUsername">Username</label>
          </div>

          <div class="form-floating mb-3">
            <input type="password" class="form-control form-control-lg" id="floatingPassword" name="password" placeholder="Password" />
            <label for="floatingPassword">Password</label>
          </div>

          <button class="btn btn-primary btn-lg w-100 mb-2" type="submit">Register</button>
        </form>

        <!-- Form Login Link -->
        <form action="/" method="get" class="w-100">
          <label class="form-check-label">Have an account?</label>
          <button class="btn btn-outline-secondary btn-lg w-100 mt-1" type="submit">Login</button>
          <label class="form-check-label text-danger"><%= locals.alert %></label>
          <p class="mt-5 text-center text-body-secondary">©2025</p>
        </form>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
  <script>
    // Paksa light mode via fallback JS
    const meta = document.querySelector('meta[name="color-scheme"]');
    if (!meta) {
      const m = document.createElement('meta');
      m.name = 'color-scheme';
      m.content = 'light only';
      document.head.appendChild(m);
    } else {
      meta.content = 'light only';
    }
  </script>
</body>
</html>
